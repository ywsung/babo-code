package com.seirion.code.util

import com.seirion.code.db.CodeData
import java.util.*

class IoUtils {
    companion object {
        fun toJson(codeData: List<CodeData>): String {
            return ""
        }

        fun fromJson(json: String): List<CodeData> {
            return Collections.emptyList()
        }
    }
}